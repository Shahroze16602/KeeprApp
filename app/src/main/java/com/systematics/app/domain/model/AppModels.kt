package com.systematics.app.domain.model

enum class ThemeMode { SYSTEM, LIGHT, DARK }

enum class PremiumOfferType { PREMIUM_1, PREMIUM_2, REMOVE_ADS }

enum class LaunchDestination { PREMIUM, LANGUAGE, ONBOARDING, HOME }
