Thick rounded-cap ring that fills with overshoot — the level/month completion meter.

```jsx
<ProgressRing value={0.72} size={120} gradient showValue label="Done" />
```

Set `gradient` for the reward gold stroke (celebration surfaces). Pass `children` for custom center content, or `showValue` for the percent.
