The KEEP / GONE / SUPER sticker that lands on a card — reusable in reviews, summaries, toasts.

```jsx
<Stamp kind="gone" size="lg" slam />
```

`slam` plays the overshoot slam-in. Rotation is baked per kind.
