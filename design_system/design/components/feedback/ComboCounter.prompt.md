The "×3 COMBO!" readout for fast consecutive swipes. Hidden below 2; grows and pops as the multiplier climbs.

```jsx
<ComboCounter combo={5} />
```

Change the `combo` prop's `key`-driven remount to replay the pop — pass a fresh value each increment.
